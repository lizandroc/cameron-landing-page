{
  "name": "cameron-landing-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute cameron-landing --remote --file=./schema.sql",
    "db:schema:local": "wrangler d1 execute cameron-landing --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
